{
  "name": "street-rush",
  "version": "1.0.0",
  "private": true,
  "description": "StreetRush cross-platform 3D racing game",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "cap:add:ios": "npx cap add ios",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:ios": "npx cap open ios",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "vite": "^7.1.0"
  }
}
